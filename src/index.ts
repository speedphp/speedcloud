// speed —— TypeScript 微服务编排框架（对标 Spring Cloud），构建于 typespeed 之上。
//
// 骨架阶段（1.3.x）：speed 自身的微服务能力（@Service / @RpcClient / @GatewayRoute / @Config 等）
// 随模块开发逐步加入；项目入口引导暂由 typespeed 的 @app / ServerFactory 承担
// （脚手架模板 src/main.ts 直接从 "typespeed" 引入，保证 MAIN_PATH 正确解析到应用目录）。
export {};
